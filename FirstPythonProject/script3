#excercise 4
def computepay(h,r):
    if h <= 40.0 :
        gross = h * r
    else
    	gross = (40 * r) + ((h - 40) * (r * 1.5
    return gross

hrs = input("Enter Hours:")
h = float(hrs)
rate = input("Enter rate:")
r = float(rate)
p = computepay(10,20)
print("Pay",p)
